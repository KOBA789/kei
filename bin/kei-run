#!/bin/bash

set -Cue

QEMU=${QEMU:-$(which qemu-system-x86_64)}

KEI_DSL=$(which kei-dsl)

CONFIG_FILE="$1"
CONFIG_DIRNAME=$(dirname "${CONFIG_FILE}")

QEMU_OPTS=$("${KEI_DSL}" "${CONFIG_BASENAME}")
echo "$QEMU_OPTS"
{
    cd "$CONFIG_DIRNAME"
    $QEMU $QEMU_OPTS
}
